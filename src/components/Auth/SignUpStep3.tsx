import { Formik } from "formik";
import { Button } from "../../atoms/Button";
import { TextInput } from "../../atoms/TextInput";

interface SignUpStep3Props {
  next: () => void;
  prev: () => void;
}

export const SignUpStep3: React.FC<SignUpStep3Props> = ({ next, prev }) => {
  return <div></div>;
};
